import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class CrowdOverallCompare extends Component {
    static propTypes = {
        crowdData: PropTypes.object.isRequired,
        userData: PropTypes.object
    }

    render() {
        return (
            <div>
                
            </div>
        )
    }
}
